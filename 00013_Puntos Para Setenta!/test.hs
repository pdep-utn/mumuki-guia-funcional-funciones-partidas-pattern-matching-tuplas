it "puntosParaSetenta 12 es 0.5" $ do
    puntosParaSetenta 12 `shouldBe` 0.5

it "puntosParaSetenta 11 es 0.5" $ do
    puntosParaSetenta 11 `shouldBe` 0.5

it "puntosParaSetenta 10 es 0.5" $ do
    puntosParaSetenta 10 `shouldBe` 0.5

it "puntosParaSetenta 1 es 5.5" $ do
    puntosParaSetenta 1 `shouldBe` 5.5

it "puntosParaSetenta 6 es 6.0" $ do
    puntosParaSetenta 6 `shouldBe` 6.0

it "puntosParaSetenta 7 es 7.0" $ do
    puntosParaSetenta 7 `shouldBe` 7.0